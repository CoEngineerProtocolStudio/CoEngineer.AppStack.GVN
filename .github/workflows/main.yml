name: Upload Co-engineer Protocol Kit

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    name: Build and Release Protocol Kit
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Build Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install build

      - name: Build Co-engineer Package
        # Senior Pivot: Target the kit directory where pyproject.toml actually lives
        run: python -m build kit/

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          # Senior Pivot: Collect the built artifacts from the kit/dist folder
          files: |
            kit/dist/*
            README.md
            protocols.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
